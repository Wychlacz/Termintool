/**
 * Central configuration for the application.
 */

/**
 * The base URL for the API backend.
 * For a production environment, this might be 'https://api.artreisen.com'.
 * For development with a proxy, '/api' is a common choice.
 */
export const API_BASE_URL = '/api';